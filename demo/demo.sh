# livedemo supports any bash command
echo "starting..."

# it supports colored output too!
exa -la

# we can tell livedemo to keep the shell open in order to interact with it after it ends
export FOO=BAR
